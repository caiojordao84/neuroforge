# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# NeuroForge RP2040 Blink Test Script
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Board: Raspberry Pi Pico (RP2040)
# Firmware: build/blink.ino.elf (Arduino)
# Protocol: G:pin=25,v=[0|1]
# UART TCP: localhost:1234
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

using sysbus

# ─────────────────────────────────────────────────────────
# 1. Criar máquina virtual RP2040
# ─────────────────────────────────────────────────────────
mach create "pico"
machine LoadPlatformDescription @platforms/cpus/rp2040.repl

# ─────────────────────────────────────────────────────────
# 2. Carregar firmware Arduino ELF
# ─────────────────────────────────────────────────────────
sysbus LoadELF @build/blink.ino.elf

# ─────────────────────────────────────────────────────────
# 3. Configurar UART0 (Serial Output via TCP)
# ─────────────────────────────────────────────────────────
# Criar terminal TCP na porta 1234
emulation CreateServerSocketTerminal 1234 "uart0" false
connector Connect sysbus.uart0 uart0

# Opcional: Logging de UART para arquivo
# logFile @uart_output.log true

# ─────────────────────────────────────────────────────────
# 4. Configurar Clock (125 MHz - RP2040 padrão)
# ─────────────────────────────────────────────────────────
sysbus.cpu0 PerformanceInMips 125

# ─────────────────────────────────────────────────────────
# 5. Mensagens de Boas-Vindas
# ─────────────────────────────────────────────────────────
echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "  🚀 NeuroForge RP2040 Blink Test"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "  Board:    Raspberry Pi Pico"
echo "  Firmware: build/blink.ino.elf (Arduino)"
echo "  UART TCP: localhost:1234"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "📡 Para conectar ao serial:"
echo "   PowerShell: .\\monitor-serial.ps1"
echo "   Netcat:     nc localhost 1234"
echo ""
echo "📋 Protocolo GPIO esperado:"
echo "   G:pin=25,v=1  (LED ON)"
echo "   G:pin=25,v=0  (LED OFF)"
echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

# ─────────────────────────────────────────────────────────
# 6. Iniciar Emulação
# ─────────────────────────────────────────────────────────
start

# ─────────────────────────────────────────────────────────
# 7. Comandos Disponíveis
# ─────────────────────────────────────────────────────────
echo "✅ Emulação iniciada!"
echo ""
echo "Comandos Renode:"
echo "  pause           - Pausar emulação"
echo "  start           - Continuar emulação"
echo "  quit            - Sair do Renode"
echo "  sysbus.cpu0 PC  - Mostrar Program Counter"
echo ""
