# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# NeuroForge RP2040 Blink Test Script
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Board: Raspberry Pi Pico (RP2040)
# Firmware: build/blink.elf (Pico SDK)
# Protocol: G:pin=25,v=[0|1]
# UART TCP: localhost:1234
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

using sysbus

# ─────────────────────────────────────────────────────────
# 1. Criar máquina RP2040
# ─────────────────────────────────────────────────────────
mach create "pico"

# Logar acessos não mapeados para debug
sysbus LogAllPeripheralsAccess true

# Carregar plataforma local (custom RP2040)
machine LoadPlatformDescription "D:/Documents/NeuroForge/server/test-firmware/rp2040/blink/platforms/raspberry-pico.repl"

# ─────────────────────────────────────────────────────────
# 2. Carregar firmware ELF
# ─────────────────────────────────────────────────────────
sysbus LoadELF "D:/Documents/NeuroForge/server/test-firmware/rp2040/blink/build/blink.elf"

# Setup do Vector Table (Ajustado para o offset do Pico SDK)
sysbus.cpu VectorTableOffset 0x10000000

# Valores extraídos do ELF (nm/readelf)
# Reset Handler: 0x100001f6 (+1 para Thumb = 0x100001f7)
# Stack Top:     0x20042000
sysbus.cpu SP 0x20042000
sysbus.cpu PC 0x100001f7

# ─────────────────────────────────────────────────────────
# 3. Configurar UART0 TCP
# ─────────────────────────────────────────────────────────
emulation CreateServerSocketTerminal 1234 "uart0-terminal" false
connector Connect sysbus.uart0 uart0-terminal

# ─────────────────────────────────────────────────────────
# 4. Configurar Clock
# ─────────────────────────────────────────────────────────
sysbus.cpu PerformanceInMips 125

# ─────────────────────────────────────────────────────────
# 5. Mensagens
# ─────────────────────────────────────────────────────────
echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "  🚀 NeuroForge RP2040 Blink Test"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "  Board:    Raspberry Pi Pico"
echo "  Firmware: build/blink.elf (Pico SDK)"
echo "  UART TCP: localhost:1234"
echo "  Platform: Custom RP2040 .repl"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "📡 Conectar ao serial:"
echo "   .\\monitor-serial.ps1"
echo ""
echo "📋 Protocolo GPIO:"
echo "   G:pin=25,v=1  (LED ON)"
echo "   G:pin=25,v=0  (LED OFF)"
echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

# ─────────────────────────────────────────────────────────
# 6. Iniciar
# ─────────────────────────────────────────────────────────
start

echo "✅ Emulação iniciada!"
echo ""
echo "Comandos:"
echo "  pause  - Pausar"
echo "  start  - Continuar"
echo "  quit   - Sair"
echo ""
