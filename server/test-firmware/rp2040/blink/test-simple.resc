# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# NeuroForge RP2040 Simple Test (No timer dependency)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Board: Raspberry Pi Pico (RP2040)
# Firmware: build/blink_simple.elf
# Protocol: G:pin=25,v=[0|1]
# UART TCP: localhost:1234
# 
# Este firmware NÃO usa sleep_ms() nem o hardware timer do RP2040
# Usa apenas loops simples para delay, funcionando bem no Renode
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

using sysbus

# ─────────────────────────────────────────────────────────
# 1. Criar máquina RP2040
# ─────────────────────────────────────────────────────────
mach create "pico"

# Carregar plataforma local
machine LoadPlatformDescription "D:/Documents/NeuroForge/server/test-firmware/rp2040/blink/platforms/raspberry-pico.repl"

# ─────────────────────────────────────────────────────────
# 2. Carregar firmware ELF (versão simples)
# ─────────────────────────────────────────────────────────
sysbus LoadELF "D:/Documents/NeuroForge/server/test-firmware/rp2040/blink/build/blink_simple.elf"

# Setup (valores extraídos do ELF com arm-none-eabi-nm)
# __StackTop = 0x20042000
# Entry point = 0x100001e9
# _reset_handler = 0x100001f6 (+1 Thumb = 0x100001f7)
sysbus.cpu0 VectorTableOffset 0x10000000
sysbus.cpu0 SP 0x20042000
sysbus.cpu0 PC 0x100001e9

# ─────────────────────────────────────────────────────────
# 3. Configurar UART0 TCP
# ─────────────────────────────────────────────────────────
emulation CreateServerSocketTerminal 1234 "uart0-terminal" false
connector Connect sysbus.uart0 uart0-terminal

# ─────────────────────────────────────────────────────────
# 4. Configurar velocidade da CPU
# ─────────────────────────────────────────────────────────
sysbus.cpu0 PerformanceInMips 125

# ─────────────────────────────────────────────────────────
# 5. Mensagens
# ─────────────────────────────────────────────────────────
echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "  🚀 NeuroForge RP2040 Simple Test"
echo "     (No timer dependency)"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "  Board:    Raspberry Pi Pico"
echo "  Firmware: build/blink_simple.elf"
echo "  UART TCP: localhost:1234"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "📡 Conectar ao serial:"
echo "   .\\monitor-serial.ps1"
echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

# ─────────────────────────────────────────────────────────
# 6. Iniciar
# ─────────────────────────────────────────────────────────
start

echo "✅ Emulação iniciada!"
echo ""
