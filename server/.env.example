# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Arduino CLI Path (optional, defaults to PATH)
ARDUINO_CLI_PATH=arduino-cli

# QEMU AVR Path (optional, defaults to PATH)
QEMU_PATH=qemu-system-avr

# Compilation Temp Directory (optional)
TEMP_DIR=/tmp/neuroforge-compile

# ============================================================================
# QEMU ESP32 Configuration
# ============================================================================

# Path to qemu-system-xtensa executable
# Leave empty to use PATH, or specify full path for custom installation
# Windows example: D:\Tools\esp-idf-tools\tools\qemu-xtensa\esp_develop_9.0.0_20240606\qemu\bin\qemu-system-xtensa.exe
# Linux/Mac example: /opt/esp-idf/tools/qemu-xtensa/bin/qemu-system-xtensa
ESP32_QEMU_PATH=qemu-system-xtensa

# QEMU Data Path (ROM binaries location: esp32_rom.bin, etc.)
# ⭐ LEAVE EMPTY FOR AUTO-DETECTION (recommended)
# The system will try to detect this automatically by:
#   1. Checking this ENV variable
#   2. Finding path relative to qemu-system-xtensa binary
#   3. Trying common installation paths by platform
#   4. Using QEMU internal paths (fallback)
#
# Only set this if auto-detection fails or you have a custom setup:
# Windows: C:\qemu-project\builds\esp32\share\qemu
# Linux:   /usr/local/share/qemu or /opt/esp-idf/qemu/share/qemu
# Mac:     /usr/local/share/qemu or /opt/homebrew/share/qemu
ESP32_QEMU_DATA_PATH=

# ESP32 Serial TCP Port (default: 5555)
# This is the port QEMU will expose for UART communication
ESP32_SERIAL_PORT=5555

# ESP32 Memory Size (default: 4M)
# Adjust if your ESP32 variant has different RAM
ESP32_DEFAULT_MEMORY=4M
